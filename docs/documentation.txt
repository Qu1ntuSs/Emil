receive:

'receive_email()' takes argument default argument 'send_reply=False' if 'send_reply=True' then it will send an email

send:

'sendmail()' takes the arguments 'subject', 'text', 'to_adress' and 'number_of_emails' 
if none of these are provided it will use default values

bot:

'bot()' takes no arguments at this point



